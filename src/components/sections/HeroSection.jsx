export default function HeroSection() {
  return (
    <section
      id="home"
      className="relative flex min-h-screen w-full flex-col items-center justify-center gap-8 overflow-hidden bg-black text-black"
    >
      {/* Cube scene, distorted wordmark, and section menu are added in later phases */}
      <p className="text-white/40 text-sm">Hero section placeholder</p>
    </section>
  );
}
